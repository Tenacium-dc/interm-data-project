# 
name: "test_log_source"

transform: |-
  .passengerid = del(.passengerid)
  .survived = del(.survived)
  .test = del(.test)

schema:
  fields:
    - name: "passengerid"
      type: "int"
    - name: "survived"
      type: "int"
    - name: "test"
      type: "int"
  ecs_field_names: []